name: test
on:
  push:
    branches:
      - main
      - master
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Install Dependencies
        run: go mod download
      - name: Start Application
        run: export СONFIG_PATH=./config/config.yaml & go run cmd/grpc-nmap/main.go & 
          sleep 5  
      - name: Run Tests
        run: ONFIG_PATH=./config/config.yaml & make test
      - name: Stop Application
        run: killall main || true
